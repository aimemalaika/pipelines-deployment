apiVersion: v1
kind: Service
metadata:
  name: budget-app-service
  namespace: budge-app-with-production
spec:
  selector:
    app: budget-app
  ports:
    - protocol: TCP
      port: 80
      targetPort: 8080
  type: ClusterIP
